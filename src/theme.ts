import {Theme} from "@emotion/react";

const color = {
  primary: 'var(--foo)',
  negative: 'red',
  positive: 'var(--blue-2)'
};


export const theme: Theme = {
  color
}


/**
 * line-height (rule of thumb): 1.5x font size
 * use values dividable by 4
 */
//


